dev env:
    windows
    amd64/x86 CPU architecture
    go 1.25.5
    dependencies for specific CPU

dev env:
    linux (asahi linux)
    arm CPU architecture
    go 1.25.5
    dependencies for specific CPU

dev env:
    macbook
    darwin/arm64 CPU architecture
    go 1.25.5
    dependencies for specific CPU

prod env:
    linux virtual machine
    ubuntu
    amd64/x86 
    go 1.25.5
    deps respective to cpu/os




install game: 

cd -> docker image
docker container -> pc which cd is installed
docker container -> pc which cd is installed
docker container -> pc which cd is installed
docker container -> pc which cd is installed



